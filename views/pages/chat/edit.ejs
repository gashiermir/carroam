<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat-Nachricht bearbeiten</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Chat-Nachricht bearbeiten</h2>
        <form action="/chat/<%= chat.id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="message">Nachricht</label>
                <textarea class="form-control" id="message" name="message" rows="5" required><%= chat.message %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Aktualisieren</button>
        </form>
        <a href="/chats/<%= chat.angebot.id %>" class="btn btn-secondary mt-3">Zurück</a>
    </div>
</body>
</html>
