<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buchung bearbeiten</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }
        .form-group label {
            font-weight: bold;
        }
        .btn-custom {
            background-color: #6D8672;
            color: #FFFFFF;
        }
        .btn-custom:hover {
            background-color: #316B5F;
            color: #FFFFFF;
        }
        .header-text {
            background-color: #316B5F;
            color: #FFFFFF;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin-bottom: 20px;
        }
        .btn-group-custom {
            display: flex;
            justify-content: space-between;
        }
    </style>
</head>
<body>
    <div class="container my-5">
            <h2>Buchung bearbeiten</h2>
        <div class="card-body">
            <form action="/buchung/<%= buchung.id %>/update" method="POST">
                <div class="form-group">
                    <label for="preis">Preis:</label>
                    <input type="number" class="form-control" id="preis" name="preis" value="<%= buchung.preis %>" required>
                </div>
                <div class="form-group">
                    <label for="status">Status:</label>
                    <select class="form-control" id="status" name="status">
                        <option value="offen" <%= buchung.status === 'offen' ? 'selected' : '' %>>Offen</option>
                        <option value="bezahlt" <%= buchung.status === 'bezahlt' ? 'selected' : '' %>>Bezahlt</option>
                        <option value="storniert" <%= buchung.status === 'storniert' ? 'selected' : '' %>>Storniert</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="buchungsdatum">Buchungsdatum:</label>
                    <input type="date" class="form-control" id="buchungsdatum" name="buchungsdatum" value="<%= buchung.buchungsdatum.toISOString().split('T')[0] %>" required>
                </div>
                <div class="form-group">
                    <label for="buchungVon">Buchung von:</label>
                    <input type="date" class="form-control" id="buchungVon" name="buchungVon" value="<%= buchung.buchungVon.toISOString().split('T')[0] %>" required>
                </div>
                <div class="form-group">
                    <label for="buchungBis">Buchung bis:</label>
                    <input type="date" class="form-control" id="buchungBis" name="buchungBis" value="<%= buchung.buchungBis.toISOString().split('T')[0] %>" required>
                </div>
                <div class="form-group">
                    <label for="mieter">Mieter:</label>
                    <select class="form-control" id="mieter" name="mieter">
                        <% mieter.forEach(function(user) { %>
                            <option value="<%= user.id %>" <%= buchung.mieter === user.id ? 'selected' : '' %>><%= user.vorname %> <%= user.nachname %></option>
                        <% }); %>
                    </select>
                </div>
                <div class="form-group">
                    <label for="angebote">Angebot:</label>
                    <select class="form-control" id="angebote" name="angebote">
                        <% angebote.forEach(function(angebot) { %>
                            <option value="<%= angebot.id %>" <%= buchung.angebote === angebot.id ? 'selected' : '' %>><%= angebot.beschreibung %></option>
                        <% }); %>
                    </select>
                </div>
                <div class="btn-group-custom">
                    <button type="submit" class="btn btn-secondary mt-3">Speichern</button>
                    <a href="/buchung" class="btn btn-primary mt-3">Zurück zur Liste</a>
                </div>
            </form>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.amazonaws.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
