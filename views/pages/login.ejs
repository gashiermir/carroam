<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        .header-container {
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .header-container a {
            position: absolute;
            left: 0;
            display: flex;
            align-items: center;
        }

        .navbar-icon {
            width: 24px;
            height: 24px;
        }
    </style>
</head>

<body>
    <div class="container my-5">
        <div class="header-container">
            <a href="/" class="arrow-container">
                <img src="/images/arrow-left-solid.svg" alt="back logo" class="navbar-icon">
            </a>
            <h2 class="text-center" style="color: black;">Login</h2>
        </div>
        <div class="card mx-auto" style="max-width: 400px; margin-top: 20px; border-color: transparent;">
            <div class="card-body">
                <form action="/auth/login" method="POST">
                    <div class="form-group">
                        <label for="email">Email:</label>
                        <input type="email" class="form-control" name="email" required />
                    </div>
                    <div class="form-group">
                        <label for="password">Password:</label>
                        <input type="password" class="form-control" name="password" required />
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Login</button>
                </form>
                <% if (errorMessage) { %>
                    <p class="error text-danger text-center mt-3"><%= errorMessage %></p>
                <% } %>
                <p class="text-center mt-3" style="color: green;">Kein Account? <a href="/signup" style="color: green;">Hier registrieren</a>.</p>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
