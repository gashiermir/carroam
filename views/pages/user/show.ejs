<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">

    <style>
        body {
            background-color: #FFFFFF; /* Weißer Hintergrund */
        }
        .btn-custom {
            background-color: #6D8672; /* Mittelgrün */
            color: #FFFFFF; /* Weißer Text */
        }
        .btn-custom:hover {
            background-color: #316B5F; /* Dunkelgrün */
            color: #FFFFFF; /* Weißer Text */
        }
        .card-header-custom {
            background-color: #ffffff; /* Hellbeige */
            color: #316B5F; /* Dunkelgrün für Text */
        }
        .card-footer-custom {
            background-color: #ffffff; /* Hellbeige */
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <div class="card">
            <div class="card-header card-header-custom">
                <h2>User Details</h2>
            </div>
            <div class="card-body">
                <p><strong>Vorname:</strong> <%= user.vorname %></p>
                <p><strong>Nachname:</strong> <%= user.nachname %></p>
                <p><strong>Email:</strong> <%= user.email %></p>
                <p><strong>Adresse:</strong> <%= user.adresse %></p>
                <p><strong>PLZ:</strong> <%= user.plz %></p>
                <p><strong>Ort:</strong> <%= user.ort %></p>
                <p><strong>Role:</strong> <%= user.role %></p>
            </div>
            <div class="card-footer card-footer-custom">
                <a class="btn btn-secondary mt-3" href="/user">Zurück zur Liste</a>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
