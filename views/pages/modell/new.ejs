<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neues Modell erstellen</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        body {
            background-color: #FFFFFF;
        }
        .btn-custom {
            background-color: #6D8672;
            color: #FFFFFF;
        }
        .btn-custom:hover {
            background-color: #316B5F;
            color: #FFFFFF;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h2 style="padding-top: 40px;">Neues Modell</h2>
        <div class="card">
            <div class="card-body">
                <form action="/modell/create" method="POST">
                    <div class="form-group">
                        <label for="marke">Marke:</label>
                        <input type="text" class="form-control" id="marke" name="markeBezeichnung" value="<%= markeBezeichnung %>" readonly>
                        <input type="hidden" name="marke" value="<%= markeId %>">
                    </div>

                    <div class="form-group">
                        <label for="name">Name:</label>
                        <input type="text" class="form-control" id="name" name="name" required>
                    </div>
                  
                    <div class="form-group">
                        <label for="beschreibung">Karosserieform:</label>
                        <select class="form-control" id="beschreibung" name="beschreibung" required>
                            <% ['SUV', 'Limousine', 'Coupé', 'Kombi', 'Cabrio', 'Pick-up', 'Van'].forEach(function(option) { %>
                                <option value="<%= option %>"><%= option %></option>
                            <% }); %>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-custom">Erstellen</button>
                    <a href="/modell" class="btn btn-secondary">Zurück zur Liste</a>
                </form>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.amazonaws.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
