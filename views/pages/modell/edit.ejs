<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modell bearbeiten</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/main.css">
    <style>
        body {
            background-color: #FFFFFF;
        }
        .btn-custom {
            background-color: #6D8672;
            color: #FFFFFF;
        }
        .btn-custom:hover {
            background-color: #316B5F;
            color: #FFFFFF;
        }
    </style>
</head>
<body>
    <h2>Modell bearbeiten</h2>
    <div class="container my-5">
        <div class="card-body">
            <form action="/modell/<%= modell.id %>/update" method="POST">
                <div class="form-group">
                    <label for="name">Name:</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= modell.name %>" required>
                </div>
                <div class="form-group">
                    <label for="baujahr">Baujahr:</label>
                    <input type="number" class="form-control" id="baujahr" name="baujahr" value="<%= modell.baujahr %>" required>
                </div>
                <div class="form-group">
                    <label for="beschreibung">Beschreibung:</label>
                    <textarea class="form-control" id="beschreibung" name="beschreibung" required><%= modell.beschreibung %></textarea>
                </div>
                <div class="form-group">
                    <label for="sitzplaetze">Sitzplätze:</label>
                    <input type="number" class="form-control" id="sitzplaetze" name="sitzplaetze" value="<%= modell.sitzplaetze %>" required>
                </div>
                <div class="form-group">
                    <label for="marke">Marke:</label>
                    <select class="form-control" id="marke" name="marke" required>
                        <% marken.forEach(function(marke) { %>
                            <option value="<%= marke.id %>" <%= modell.marke && modell.marke.id === marke.id ? 'selected' : '' %>><%= marke.bezeichnung %></option>
                        <% }); %>
                    </select>
                </div>
                <button type="submit" class="btn btn-secondary mt-3">Speichern</button>
                <a href="/modell" class="btn btn-primary mt-3">Zurück zur Liste</a>
            </form>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
